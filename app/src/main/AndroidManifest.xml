<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RememHub"
        tools:targetApi="31">

        <!-- Actividades del primer archivo -->
        <activity
            android:name=".activity_resumen"
            android:exported="false" />
        <activity
            android:name=".activity_historial"
            android:exported="false" />
        <activity
            android:name=".activity_configuracion"
            android:exported="false" />
        <activity
            android:name=".detalles_de_tarea"
            android:exported="false" />
        <!-- Categorias -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".menu_inicio"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".tareas_pendientes_inicio"
            android:exported="true" />
        <activity
            android:name=".PapeleraActivity"
            android:exported="false" />

        <!-- Actividades adicionales del segundo archivo -->
        <activity
            android:name=".VerTareasxCategorias"
            android:exported="false" />
        <activity
            android:name=".Añadirtarea"
            android:exported="true"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".VerActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity2"
            android:exported="false"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".EditarActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Meta-data para PARENT_ACTIVITY -->
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".MainActivity" />

    </application>
</manifest>
